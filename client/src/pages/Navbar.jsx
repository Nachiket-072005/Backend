import { Button } from "@/components/ui/button";
import React from "react";

const Navbar = () => {
  return (
    <div>
      <h1>{"Nachiket's Bar"}</h1>
      <Button>Logout</Button>
    </div>
  );
};

export default Navbar;
